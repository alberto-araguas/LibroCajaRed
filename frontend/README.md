# Frontend

Frontend React con Vite del Libro de Caja.

## Validar build

Desde la raiz del proyecto:

```bash
docker compose exec -T frontend npm run build
```

## Variables

- `VITE_API_URL`: URL del backend. Por defecto, `http://localhost:8000`.
